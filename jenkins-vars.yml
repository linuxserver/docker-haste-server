---

# jenkins variables
project_name: docker-haste-server
external_type: github_stable
release_type: stable
release_tag: latest
ls_branch: master
repo_vars:
  - EXT_GIT_BRANCH = 'master'
  - EXT_USER = 'seejohnrun'
  - EXT_REPO = 'haste-server'
  - BUILD_VERSION_ARG = 'HASTE_RELEASE'
  - LS_USER = 'linuxserver'
  - LS_REPO = 'docker-haste-server'
  - CONTAINER_NAME = 'haste-server'
  - DOCKERHUB_IMAGE = 'linuxserver/haste-server'
  - DEV_DOCKERHUB_IMAGE = 'lsiodev/haste-server'
  - PR_DOCKERHUB_IMAGE = 'lspipepr/haste-server'
  - DIST_IMAGE = 'alpine'
  - MULTIARCH='true'
  - CI='true'
  - CI_WEB='true'
  - CI_PORT='80'
  - CI_SSL='false'
  - CI_DELAY='120'
  - CI_DOCKERENV='TZ=US/Pacific'
  - CI_AUTH='user:password'
  - CI_WEBPATH=''
